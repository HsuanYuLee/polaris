{
  "name": "polaris-framework-workspace",
  "version": "4.59.3",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "viewer:dev": "pnpm --dir docs-manager dev",
    "viewer:build": "pnpm --dir docs-manager build",
    "viewer:preview": "pnpm --dir docs-manager preview"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.11"
  }
}
