# Library Change Protocol

> This file has moved to `skills/references/library-change-protocol.md` (loaded on-demand when a dependency change is being considered).

The full protocol — default stance, three-layer investigation, replacement/upgrade evaluation, decision tiers, reviewer-suggested upgrades, handbook integration, config elimination, workaround documentation standard, and common rationalizations — is in `skills/references/library-change-protocol.md`.

**When to load**: any task involving replacing, upgrading, or removing a dependency.
