[post-create]
env = """
cp {{ primary_worktree_path }}/.env {{ worktree_path }}/.env 2>/dev/null || true
cp {{ primary_worktree_path }}/apps/main/.env {{ worktree_path }}/apps/main/.env 2>/dev/null || true
"""
install = "pnpm install"

[pre-merge]
lint = "pnpm run lint"
test = "pnpm run test:unit"
