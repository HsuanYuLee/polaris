# Polaris workspace .gitignore
#
# 這個檔案不只是排除清單，它同時是**分類宣告**。repo 裡沒有被版控的東西只有三類：
# 在別的 repo 裡被版控的、可以重新產生的、只屬於這台機器的。每一條規則都住在它那一類的
# 標頭底下，`gate-ignore-classes.sh` 會檢查兩件事——沒有規則指向不存在的東西，也沒有
# 存在的忽略路徑落在任何一類之外。
#
# 不用 whitelist 模式（`*` 之後逐條 `!`）。那個模式下每一條排除規則都被 `*` 遮住，
# 你沒辦法機械地分辨哪一條還活著、哪一條在防一個已經不存在的機制——2026-08-03 實測
# 89 條規則裡有 34 條拿掉之後判定完全不變。看不出死活的規則會一直累積。

# === class: versioned-elsewhere ===
# 有自己的 git repo。內容不歸這個 repo 管，但它們是被版控的，不是殘留。

# 每張單記的是這個人在做什麼、為什麼這樣定義成功；換一個人用同一套框架，這裡完全不一樣。
# 框架只提供空殼（_template/issues/）。它仍然必須是 git repo——凍結 ＝ commit，歷史是唯一
# 一個寫在檔案裡的人改不掉的竄改證據。
/issues/

# 舊交付層的 spec 容器，自己是一個 repo（194 MB / 7291 檔）。DP-462 之後沒有人往裡面寫，
# 但它不是無主的散檔。
/docs-manager/src/content/docs/specs/

# === class: machine-local ===
# 只存在於這台機器：公司內容、本機設定、跨 session 的本機狀態。
# 它們不進版控是對的，不是殘留。

# 公司目錄按名字逐個列，不用 `*/polaris-config/` 這種形狀規則。「頂層任何一個資料夾都算
# 公司」是用位置判斷身分，多一個資料夾就多一個看不見的豁免；名字寫出來才會出現在 diff 裡。
/kkday/
/user/
/workspace-config.yaml
/.vscode/
/.daily-triage.json
/.claude/settings.local.json
/.claude/active-thread.md
/.claude/checkpoints/
/.claude/projects/

# === class: regenerable ===
# 刪掉之後跑一次就會回來的執行期產物。

/.polaris/
/.pnpm-store/
/.pytest_cache/
/.ruff_cache/
/docs-manager/.astro/
/docs-manager/dist/
/docs-manager/public/evidence/

# === class: recurring ===
# 不綁特定路徑的一類東西，會在任何地方反覆長出來。這一類豁免「目標必須存在」的檢查，
# 因為它們防的是還沒發生的那一次。

node_modules/
__pycache__/
*.pyc
.DS_Store
.vite/
# 排程任務跑的時候才長出來的鎖檔。它綁的是那一次執行，不是一個必須存在的位置——
# 放在 machine-local 底下的話，這道閘在「現在沒有排程在跑」的每一台機器上都是紅的。
/.claude/scheduled_tasks.lock
